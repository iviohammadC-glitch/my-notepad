# فعال‌کردن حساب کاربری DailyPad با Supabase

این نسخه کد ثبت‌نام، ورود، تأیید ایمیل، بازیابی و تغییر رمز، خروج، ذخیرهٔ ابری و انتقال داده‌های محلی را دارد. برای فعال‌شدن حساب واقعی، پروژهٔ Supabase خودت را متصل کن. بدون تنظیم اتصال، دکمه‌های حساب عمداً غیرفعال‌اند و پیام واضح نمایش داده می‌شود؛ «ورود به فضای محلی» همچنان قابل استفاده است.

## ۱. اول از اطلاعات قبلی پشتیبان بگیر

در نسخهٔ فعلی دیلی‌پد: تنظیمات ← خروجی JSON. فایل را نگه دار. اطلاعات آدرس `localhost` با اطلاعات آدرس `192.168.1.101` جداست؛ پشتیبان را از همان آدرسی بگیر که قبلاً با آن کار می‌کردی.

ZIP جدید را در یک پوشهٔ تازه Extract کن. در VS Code پوشهٔ DailyPad جدید را باز کن (پوشه‌ای که package.json داخلش است).

```bash
npm install
```

## ۲. پروژهٔ Supabase بساز

1. وارد https://supabase.com/dashboard شو و حساب بساز / وارد شو.
2. New project را انتخاب کن. اگر پلن می‌خواهد Free را انتخاب کن؛ پلن رایگان سقف مصرف دارد.
3. نام پروژه را DailyPad بگذار، یک رمز قوی برای دیتابیس تعیین کن و منطقهٔ مناسب را انتخاب کن.
4. صبر کن ساخت پروژه تمام شود.

رمز دیتابیس را در برنامهٔ React، فایل .env فرانت‌اند یا چت قرار نده.

## ۳. جدول‌ها و دسترسی امن را بساز

1. در داشبورد Supabase بخش SQL Editor را باز کن و New query بزن.
2. فایل `supabase/schema.sql` داخل پروژهٔ DailyPad را در VS Code باز کن.
3. تمام محتوای آن را در SQL Editor کپی کن و Run بزن.
4. در Table Editor، جدول `workspaces` را می‌بینی. RLS باید فعال باشد.

این اسکریپت به هر حساب فقط اجازهٔ خواندن و نوشتن دادهٔ خودش را می‌دهد. RLS را غیرفعال نکن. رمزها توسط Supabase Auth مدیریت می‌شوند؛ برنامه جدول رمز سفارشی ندارد.

## ۴. تنظیمات عمومی اتصال را بردار

در بخش Connect یا Settings / API Keys پروژه، این دو مقدار را پیدا کن:

- Project URL، مثل `https://abcdefgh.supabase.co`
- Publishable key با پیشوند `sb_publishable_`؛ کلید قدیمی `anon` نیز پشتیبانی می‌شود.

فایل `.env.example` را در VS Code کپی کن و نام نسخهٔ کپی را `.env` بگذار. این فایل باید کنار package.json باشد، نه داخل src. محتوا را با مقادیر واقعی پروژهٔ خودت پر کن:

```env
VITE_SUPABASE_URL=https://abcdefgh.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_کلید_عمومی_واقعی
```

اگر پروژه فقط کلید legacy anon دارد، به‌جای خط دوم بنویس:

```env
VITE_SUPABASE_ANON_KEY=کلید_anon_واقعی
```

مقادیر نمایشی بالا کار نمی‌کنند؛ باید مقادیر داشبورد خودت را استفاده کنی.

**مهم:** `service_role`، `sb_secret_`، رمز دیتابیس و رمز SMTP محرمانه‌اند و نباید وارد فرانت‌اند شوند. کلید publishable عمومی است و امنیت اطلاعات به RLS بستگی دارد، نه پنهان‌کردن این کلید.

## ۵. ورود با ایمیل و آدرس برگشت را تنظیم کن

در Authentication، بخش Sign In / Providers (یا Providers) را باز کن:

- Email را فعال نگه دار.
- ثبت‌نام کاربران جدید مجاز باشد.
- Confirm email را برای استفادهٔ واقعی روشن نگه دار.
- اگر تنظیم سیاست رمز در دسترس است، حداقل طول ۱۰ نویسه و ترکیب حرف و عدد را در سمت سرویس هم اعمال کن. فرم برنامه همین حداقل را بررسی می‌کند؛ بررسی مرورگر جای سیاست سمت سرور را نمی‌گیرد.

در Authentication → URL Configuration:

برای تست روی همان کامپیوتر:

```text
Site URL:
http://localhost:5173/

Redirect URLs:
http://localhost:5173/**
```

اگر با همان آدرس شبکهٔ قبلی کار می‌کنی، این مورد را هم به Redirect URLs اضافه کن:

```text
http://192.168.1.101:5173/**
```

اگر IP یا پورت تغییر کرد، آدرس مجاز را هم به‌روز کن. لینک localhost باید روی همان کامپیوتر اجراکنندهٔ Vite باز شود؛ روی گوشی، localhost به خود گوشی اشاره می‌کند.

قالب‌های پیش‌فرض Supabase برای Confirm signup و Reset password را نگه دار. لینک اصلی ایمیل باید از `{{ .ConfirmationURL }}` استفاده کند. این برنامه یک SPA با جریان implicit است و بازگشت از ایمیل را از fragment نشانی مدیریت می‌کند؛ قالب‌های نمونهٔ سرورمحور با مسیر `/auth/confirm` را بدون پیاده‌سازی سرور جایگزین نکن.

## ۶. محدودیت مهم ایمیل — قبل از تست بخوان

سرویس پیش‌فرض ایمیل Supabase برای استفادهٔ عمومی طراحی نشده است. طبق مستندات رسمی، بدون SMTP اختصاصی فقط به آدرس‌های مجاز اعضای تیم پروژه ایمیل می‌فرستد و محدودیت ارسال کمی دارد. برای تست اولیه از همان ایمیلی که عضو تیم Supabase است استفاده کن.

برای ثبت‌نام و بازیابی رمز کاربران دیگر:

1. یک سرویس ارسال ایمیل با SMTP انتخاب کن.
2. فرستنده / دامنهٔ خودت را طبق دستور سرویس تأیید کن.
3. تنظیمات آن را فقط در Authentication → SMTP Settings در Supabase وارد کن.
4. یک ایمیل تأیید و یک بازیابی رمز واقعی را تست کن.

محدودیت‌ها و هزینهٔ SMTP به سرویس انتخابی وابسته است؛ رایگان و نامحدود بودن آن تضمین نشده. برای دورزدن مشکل ارسال، تأیید ایمیل را در نسخهٔ عمومی خاموش نکن.

مستندات رسمی:
- https://supabase.com/docs/guides/auth/auth-smtp
- https://supabase.com/docs/guides/auth/redirect-urls
- https://supabase.com/docs/guides/database/postgres/row-level-security

## ۷. اجرا و ثبت‌نام

اگر سرور قبلی باز است، Ctrl+C بزن؛ برای جلوگیری از تغییر پورت آن را ببند. سپس در پوشهٔ جدید:

```bash
npm run dev
```

http://localhost:5173/ را باز کن. پیام «اتصال حساب کاربری هنوز تنظیم نشده» باید حذف شده باشد.

1. روی «ساخت حساب» بزن.
2. نام، ایمیل، رمز و تکرار رمز را بنویس.
3. لینک تأیید ایمیل را باز کن.
4. وارد حساب شو و یک کار / یادداشت بساز.
5. منتظر پیام «ذخیره‌شده در ابر» بمان.
6. صفحه را رفرش کن؛ داده باید باقی بماند.
7. در Settings حساب، ایمیل و دکمه‌های تغییر رمز و خروج را می‌بینی.

## ۸. انتقال اطلاعات قبلی

اگر روی همان آدرس و مرورگر هستی، در تنظیمات حساب گزینهٔ «انتقال اطلاعات محلی» نمایش داده می‌شود. ایمیل مقصد را بررسی و انتقال را تأیید کن. نسخهٔ محلی حذف نمی‌شود و در شناسه‌های تکراری، نسخهٔ فعلی حساب حفظ می‌شود.

اگر آدرس / مرورگر عوض شده، از تنظیمات ← ورود پشتیبان، فایل JSON قبلی را وارد کن و ادغام را انتخاب کن.

برای رفتن از حساب ابری به حالت محلی، ابتدا از حساب خارج شو؛ حالت محلی حساب کاربری نیست و به ابر وصل نمی‌شود.

## ۹. همگام‌سازی و تعارض

تغییرات بعد از وقفهٔ کوتاه در تایپ به Supabase ارسال می‌شوند. در مرورگر نیز کش جداگانه برای هر شناسهٔ کاربر وجود دارد. اگر اینترنت قطع شود، تغییرات ارسال‌نشده با هشدار نگه داشته می‌شوند؛ پس از وصل‌شدن یا فشردن «تلاش دوباره» دوباره ارسال می‌شوند.

داده‌های دستگاه دیگر در ورود / بازکردن برنامه یا با «تازه‌سازی» دریافت می‌شوند؛ این نسخه ویرایش هم‌زمان real-time ندارد. اگر دو دستگاه روی نسخهٔ قبلی کار کنند، نسخهٔ قدیمی اجازهٔ بازنویسی بی‌صدا ندارد: هشدار تعارض می‌گیری. ابتدا «پشتیبان این نسخه»، سپس «تازه‌سازی» را بزن و در صورت نیاز پشتیبان را از تنظیمات ادغام کن.

خروج عادی ابتدا تلاش می‌کند تغییرات را ذخیره کند؛ اگر ذخیره شکست بخورد، خروج متوقف می‌شود تا تغییراتت بی‌صدا از دست نرود. پس از خروج موفق، کش این حساب از مرورگر پاک می‌شود؛ داده‌های ابری و فضای محلی قدیمی باقی می‌مانند.

## ۱۰. انتشار با Netlify

این بار هم فایل‌های فرانت‌اند روی Netlify قرار می‌گیرند و Supabase سرور حساب‌ها و داده‌هاست؛ سرور Node.js جدا لازم نیست.

1. فایل .env محلی را با مقادیر عمومی واقعی آماده کن.
2. `npm run build` را اجرا کن؛ مقادیر عمومی هنگام build در خروجی قرار می‌گیرند.
3. پوشهٔ dist را روی Netlify آپلود کن؛ .env، سورس و فایل‌های محرمانه را آپلود نکن.
4. Site URL پروژهٔ Supabase را آدرس HTTPS نهایی سایتت قرار بده.
5. Redirect URLs دقیق نهایی را اضافه کن، مثل:

```text
https://نام-سایت-خودت.netlify.app/
https://نام-سایت-خودت.netlify.app/?mode=recovery
```

برای دامنهٔ عمومی از wildcard گستردهٔ همهٔ netlify.app استفاده نکن. اگر تنظیمات .env را تغییر بدهی باید دوباره build و آپلود کنی.

## محدودهٔ تست و پیش‌نیاز انتشار

کد frontend، حالت محلی و فرم‌ها با تست مرورگر بررسی می‌شوند. SQL و سیاست‌های جداسازی کاربران در PostgreSQL محلی آزمایش می‌شوند. تا پروژه و SMTP واقعی خودت متصل نباشند، ارسال ایمیل و ورود به سرویس واقعی قابل تأیید نیست. پیش از عمومی‌کردن، با دو حساب مجزا بررسی کن داده‌های یکدیگر را نمی‌بینند و تأیید ایمیل / بازیابی رمز واقعی درست عمل می‌کند.

اعلان‌های سیستم همچنان به اجازهٔ مرورگر و بستر امن نیاز دارند و یادآوری‌ها فقط هنگام باز بودن برنامه بررسی می‌شوند. این تغییر حساب‌ها را اضافه می‌کند، نه پوش‌نوتیفیکیشنِ برنامهٔ بسته.
